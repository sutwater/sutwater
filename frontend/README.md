# Frontend — Smart Water Meter SUTH

React 19 + TypeScript + Vite + Tailwind CSS 4 + Ant Design 5

## Commands

```bash
npm install       # ติดตั้ง dependencies
npm run dev       # dev server → http://localhost:5173
npm run build     # production build (tsc + vite)
npm run lint      # ESLint
```

## Environment

สร้างไฟล์ `.env` ก่อนรัน:

```env
VITE_API_BASE_URL=http://localhost:8000/api/v1
```

ดูรายละเอียดเพิ่มเติมที่ [README หลัก](../README.md)
